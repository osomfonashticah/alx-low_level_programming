#include "main.h"
#include<stdio.h>

/**
 * main - function that multiplies two integers
 * @argc: argument count
 * @argv:argument array
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	int a, b, result, i;

	if (!argv[i])
		printf("Error");
	result = a * b;
	printf("%d\n", result);
	return (0);
}
